<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aplica.andres.adminsqlite">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Centro TIC"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity
            android:name=".SplashScreen"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Login"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity android:name=".Frag_Perfil" />
        <activity android:name=".Equipos" />
        <activity android:name=".Registro" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".Estudiante"
            android:label="@string/title_activity_estudiante"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity android:name=".Lista" />
        <activity android:name=".Salas" />
        <activity android:name=".Prestamo" />
        <activity android:name=".Administrador"></activity>
    </application>

</manifest>